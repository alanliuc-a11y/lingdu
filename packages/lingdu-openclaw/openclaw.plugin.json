{
  "id": "lingdu",
  "name": "LingDu",
  "description": "Cross-device memory synchronization for AI assistants. Syncs SOUL.md, USER.md, and MEMORY.md files to cloud storage with end-to-end encryption. Requires user authorization via browser-based device code flow. Background daemon monitors file changes and syncs automatically.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "cloud_url": { "type": "string" },
      "email": { "type": "string" },
      "token": { "type": "string" }
    }
  },
  "uiHints": {
    "cloud_url": { "label": "服务器地址 / Server URL", "placeholder": "https://soulsync.work" },
    "email": { "label": "邮箱 / Email" },
    "token": { "label": "Token", "sensitive": true }
  }
}
